<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flash_screen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/payremanider1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".clock"></activity>
        <activity android:name=".alarm" />
        <activity
            android:name=".Update_Password"
            android:label="@string/title_activity_update__password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".what_is_pay_remainder" />
        <activity android:name=".rate_activity" />
        <activity android:name=".todolist" />
        <activity android:name=".payoptions" />
        <activity android:name=".calender" />
        <activity
            android:name=".mainpage"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity android:name=".Registration" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>